<%= form_with(model: lunch, local: true) do |form| %>
  <% if lunch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lunch.errors.count, "error") %> prohibited this lunch from being saved:</h2>

      <ul>
      <% lunch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :t %>
    <%= form.text_field :t, id: :lunch_t %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :lunch_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :lunch_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :lunch_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
