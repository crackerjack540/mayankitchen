<%= form_with(model: dip, local: true) do |form| %>
  <% if dip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dip.errors.count, "error") %> prohibited this dip from being saved:</h2>

      <ul>
      <% dip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :dip_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :dip_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :dip_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
