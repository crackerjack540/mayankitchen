<p id="notice"><%= notice %></p>

<h1 class="menu-title">Dinner Menu</h1>

<h2 class="menu-head">Antojiotos (Appetizers)</h2>

  <% @appetizers.each do |appetizer| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= appetizer.title %></p></th>
      <th><p class="price">$<%= appetizer.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= appetizer.description %></p></th>
    </tr>
  </table>
  <% end %>

<h2 class="menu-head">Tapas</h2>

  <% @tapas.each do |tapa| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= tapa.title %></p></th>
      <th><p class="price">$<%= tapa.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= tapa.description %></p></th>
    </tr>
  </table>
  <% end %>


<h2 class="menu-head">Sopas (Soups)</h2>

  <% @soups.each do |soup| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= soup.title %></p></th>
      <th><p class="price"><%= soup.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= soup.description %></p></th>
    </tr>
  </table>
  <% end %>


<h2 class="menu-head">Favorite Dips</h2>
<h4>Served with Tri Color Tortilla Chips</h4>

  <% @dips.each do |dip| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= dip.title %></p></th>
      <th><p class="price">$<%= dip.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= dip.description %></p></th>
    </tr>
  </table>
  <% end %>

<h2 class="menu-head">Nachos</h2>

  <% @nachos.each do |nacho| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= nacho.title %></p></th>
      <th><p class="price">$<%= nacho.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= nacho.description %></p></th>
    </tr>
  </table>
  <% end %>


<h2 class="menu-head">Ensaladas (Salads)</h2>

  <% @salads.each do |salad| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= salad.title %></p></th>
      <th><p class="price">$<%= salad.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= salad.description %></p></th>
    </tr>
  </table>
  <% end %>

<h2 class="menu-head">Bisteks (Steaks)</h2>

  <% @steaks.each do |steak| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= steak.title %></p></th>
      <th><p class="price">$<%= steak.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= steak.description %></p></th>
    </tr>
  </table>
  <% end %>

<h2 class="menu-head">Pollo (Chicken)</h2>

  <% @chickens.each do |chicken| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= chicken.title %></p></th>
      <th><p class="price">$<%= chicken.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= chicken.description %></p></th>
    </tr>
  </table>
  <% end %>

<h2 class="menu-head">Cerdo (Pork)</h2>

  <% @porks.each do |pork| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= pork.title %></p></th>
      <th><p class="price">$<%= pork.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= pork.description %></p></th>
    </tr>
  </table>
  <% end %>


<h2 class="menu-head">Mariscos (Seafood)</h2>

  <% @seafoods.each do |seafood| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= seafood.title %></p></th>
      <th><p class="price">$<%= seafood.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= seafood.description %></p></th>
    </tr>
  </table>
  <% end %>


<h2 class="menu-head">Side Dishes</h2>

  <% @sides.each do |side| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= side.title %></p></th>
      <th><p class="price">$<%= side.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= side.description %></p></th>
    </tr>
  </table>
  <% end %>


<h1 class="menu-title"> Otras Especilidades </br> (Other Specialties)</h1>

<h2 class="menu-head">Fajitas</h2>

  <% @fajitas.each do |fajita| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= fajita.title %></p></th>
      <th><p class="price">$<%= fajita.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= fajita.description %></p></th>
    </tr>
  </table>
  <% end %>


<h2 class="menu-head">Chimichangas</h2>

  <% @chimichangas.each do |chimichanga| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= chimichanga.title %></p></th>
      <th><p class="price">$<%= chimichanga.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= chimichanga.description %></p></th>
    </tr>
  </table>
  <% end %>

<h2 class="menu-head">Tacos</h2>

  <% @tacos.each do |taco| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= taco.title %></p></th>
      <th><p class="price">$<%= taco.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= taco.description %></p></th>
    </tr>
  </table>
  <% end %>

<h2 class="menu-head">Quesadillas</h2>

  <% @quesadillas.each do |quesadilla| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= quesadilla.title %></p></th>
      <th><p class="price">$<%= quesadilla.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= quesadilla.description %></p></th>
    </tr>
  </table>
  <% end %>

<h2 class="menu-head">Enchiladas</h2>

  <% @enchiladas.each do |enchilada| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= enchilada.title %></p></th>
      <th><p class="price">$<%= enchilada.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= enchilada.description %></p></th>
    </tr>
  </table>
  <% end %>

<h2 class="menu-head"> Bebidas (Drinks)</h2>

  <% @drinks.each do |drink| %>
  <table class="menu">
    <tr>
      <th><p class="card-title"><%= drink.title %></p></th>
      <th><p class="price">$<%= drink.price %></p></th>
    </tr>
    <tr>
      <th ><p class="description"><%= drink.description %></p></th>
    </tr>
  </table>
  <% end %>
