<%= form_with(model: chicken, local: true) do |form| %>
  <% if chicken.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chicken.errors.count, "error") %> prohibited this chicken from being saved:</h2>

      <ul>
      <% chicken.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :chicken_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :chicken_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :chicken_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
