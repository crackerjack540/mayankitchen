<%= form_with(model: enchilada, local: true) do |form| %>
  <% if enchilada.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enchilada.errors.count, "error") %> prohibited this enchilada from being saved:</h2>

      <ul>
      <% enchilada.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :enchilada_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :enchilada_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :enchilada_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
