<%= form_with(model: nacho, local: true) do |form| %>
  <% if nacho.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nacho.errors.count, "error") %> prohibited this nacho from being saved:</h2>

      <ul>
      <% nacho.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :nacho_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :nacho_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :nacho_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
