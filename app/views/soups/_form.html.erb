<%= form_with(model: soup, local: true) do |form| %>
  <% if soup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(soup.errors.count, "error") %> prohibited this soup from being saved:</h2>

      <ul>
      <% soup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :soup_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :soup_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :soup_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
